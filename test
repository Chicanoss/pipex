tube pere 4

test test test
test


				while (path[j])
				{	
					args = ft_split(argv[j], ' ');
					tmp = ft_strjoin(path[j], args[0]);
					free(args[0]);
					args[0] = tmp;
					execve(args[0], args, env);
					j++;
	}
				//care_child(path, argv, env, i);

							dup2(tube[1], 0);
			dup2(fd, 1);